#' @section Cache Family of Classes:
#' The Cache family of classes provide memory management by storing memory
#' intensive components of objects, such as large text objects to file.
#' The Cache family is comprised of a CacheManager class that accepts
#' requests to read and write objects to the cache.  The actual cache
#' input and output is managed by the Cache class which maintains the
#' objects in the cache.
